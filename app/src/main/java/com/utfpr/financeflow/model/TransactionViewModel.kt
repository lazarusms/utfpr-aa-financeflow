package com.utfpr.financeflow.model

import androidx.lifecycle.ViewModel

class TransactionViewModel : ViewModel() {
    //TODO verificar campos/tipos
    fun saveTransaction(
        date: String,
        category: String,
        amount: String,
        description: String
    ) {
    }
}
